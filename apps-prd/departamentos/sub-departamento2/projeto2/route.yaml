apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: app-demo2
  labels:
    app: app-demo2
spec:
  to:
    kind: Service
    name: app-demo2
    weight: 100
  port:
    targetPort: 8080
  wildcardPolicy: None
