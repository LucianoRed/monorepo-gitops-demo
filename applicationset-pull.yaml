apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: pull-app-demo-set
  namespace: openshift-gitops
spec:
  generators:
    - matrix:
        generators:
          - clusters:
              selector:
                matchLabels:
                  env: spoke
          - git:
              repoURL: https://github.com/LucianoRed/monorepo-gitops-demo.git
              revision: HEAD
              directories:
                - path: apps-prd/apps-argo/*
  template:
    metadata:
      name: '{{path.basename}}-{{name}}'
    spec:
      project: default
      source:
        repoURL: https://github.com/LucianoRed/monorepo-gitops-demo.git
        targetRevision: HEAD
        path: '{{path}}'
      destination:
        server: '{{server}}'
        namespace: '{{path.basename}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
